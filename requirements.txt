# requirements.txt
# Backend, gateway and tools. No hardware is needed to install or test any of this.

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.6,<3.0
cryptography>=42.0
bleak>=0.21,<1.0          # BLE on the Pi gateway; unused by the backend and the tests
pyyaml>=6.0
pytest>=8.0
httpx>=0.27               # TestClient transport, and the reporter's HTTP client
